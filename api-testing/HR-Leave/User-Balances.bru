meta {
  name: User Balances
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/hr/leave/balances/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Cookie: auth_token={{authToken}}
}

docs {
  Another employee's balances — self, a subtree manager, hr_admin or org_admin
  only. Same row shape and same optional ?as_of= as My Balances.
}
